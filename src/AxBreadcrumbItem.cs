using Avalonia.Controls;

namespace ArxisStudio.Controls;

/// <summary>
/// Звено хлебных крошек: подпись с шевроном-разделителем слева. Последнее
/// звено принято помечать классом <c>current</c> — оно читается основным
/// текстом, остальные ведут себя как ссылки; класс <c>error</c> красит звено
/// с проблемой.
/// </summary>
public class AxBreadcrumbItem : ContentControl
{
}

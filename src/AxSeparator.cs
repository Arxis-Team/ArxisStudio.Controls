using Avalonia.Controls;

namespace ArxisStudio.Controls;

/// <summary>
/// Черта между группами пунктов меню: у расширения — вместо <see cref="Separator"/>.
/// </summary>
/// <remarks>
/// Родной разделитель Avalonia расширению заводить нельзя — интерфейс плагина строится на <c>Ax*</c>
/// (<c>ARX0001</c>), — и меню расширения до сих пор делило группы разве что цветом пункта. Этот —
/// тот же разделитель, одетый темой так же: линия в пиксель с воздухом по бокам. В меню он строка
/// сама по себе, а не содержимое пункта: и в корне, и в подменю <see cref="AxMenuItem"/>.
/// </remarks>
public class AxSeparator : Separator
{
}
